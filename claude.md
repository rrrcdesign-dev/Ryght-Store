# Project Constitution

This file acts as the project constitution, detailing data schemas, behavioral rules, and architectural invariants.

## Data Schemas
*(To be defined once user requirements are clear)*

## Behavioral Rules
1. Never assume or guess business logic.
2. Prioritize reliability over speed.
3. Test all integrations before coding core features.

## Architectural Invariants
1. Layer 1 (Architecture): Markdown SOPs in `architecture/`.
2. Layer 2 (Navigation): LLM routing logic.
3. Layer 3 (Tools): Deterministic, atomic, testable Python scripts in `tools/` using `.tmp/` for intermediates.
